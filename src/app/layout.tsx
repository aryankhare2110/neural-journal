import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  variable: '--font-inter',
  subsets: ['latin'],
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Neural Journal — 3D Spatial Journaling',
  description:
    'A spatial journaling system that maps your thoughts as glowing nodes in 3D space. Visualize memory, sentiment, and the passage of time.',
  keywords: ['journal', '3D', 'spatial', 'mental health', 'thoughts', 'visualization'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
